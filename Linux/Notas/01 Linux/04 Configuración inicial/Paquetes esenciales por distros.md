## Idea clave:
Siempre instalar el mismo set para trabajar cómodo

## Incluye:
- compilación 
- red
- edicion
- python
- nodejs
- monotoreo

### 📦 Imprescindibles

```bash
# Debian / Ubuntu
sudo apt install -y \
  build-essential curl wget git vim nano \
  ca-certificates unzip zip htop \
  python3 python3-pip \
  openssh rsync tmux neofetch
```

```bash 
# Arch
sudo pacman -S --needed \
  base-devel curl wget git vim nano \
  unzip zip htop python python-pip \
  openssh rsync tmux neofetch
```

```bash 
# Fedora
sudo dnf install -y \
  @development-tools curl wget git vim nano \
  unzip zip htop python3 python3-pip \
  openssh rsync tmux neofetch
```

## Relacionado:
[[Actualización sistema en Linux]]

